import { HighvoteDirective } from './highvote.directive';

describe('HighvoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighvoteDirective();
    expect(directive).toBeTruthy();
  });
});
